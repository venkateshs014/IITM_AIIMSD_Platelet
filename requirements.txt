# Phase 1 - automated pre-annotation & quantitative feature extraction
opencv-python>=4.5
numpy>=1.21
scipy>=1.7
scikit-image>=0.19
scikit-learn>=1.0
pandas>=1.3
matplotlib>=3.4
seaborn>=0.11
openpyxl>=3.0

# Phase 2 (deep learning) dependencies are intentionally omitted here; the
# learned segmentation/grading models will ship with their own requirements.
# Reference stack from the paper: torch, torchvision, albumentations.
